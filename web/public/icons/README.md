Rasterised from the brand mark in `public/favicon.svg` (the same paths as
useneptune.org's logo) on the page gradient: `icon-192.png` and
`icon-512.png` are rounded tiles for the `any` purpose, and
`icon-maskable-512.png` is full bleed with the mark inside the safe zone for
Android's adaptive icons. Re-render with a canvas (see git history of this
file) if the mark or the palette changes.
