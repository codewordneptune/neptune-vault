{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The window shows the wallet and calls the bridge. It needs nothing else: no filesystem, no shell, no network plugin. Everything the wallet does with the node goes through the page's own fetch, under the CSP in tauri.conf.json.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
