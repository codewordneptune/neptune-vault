[package]
name = "vault-fixtures"
version = "0.1.0"
edition = "2021"
publish = false
description = "Native tool that writes deterministic PrimitiveWitness fixtures for the browser prover benchmark"

[dependencies]
anyhow = "1"
bincode = "1.3"
neptune-consensus = { version = "0.17.0", features = ["test-helpers"] }
proptest = "1"
